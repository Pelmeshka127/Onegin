#ifndef ONEGIN_H_
#define ONEGIN_H_

void Copy_File (FILE * old, FILE * nw);

int Num_Of_Strings (FILE * old);

int Max_String_Len (FILE * old);

void Sorted_Strings (char *strings[], int ch, FILE * nw);

#endif
